(jbuild_version 1)

(library
 ((name        dokeysto)
  (public_name dokeysto)
  (modules (db))
  (libraries (dbm unix lz4))))

(executable
  ((name test)
   (modules (test))
   (libraries (dokeysto))))
